services:
  cuda_env:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/ws
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: $cmd


